/*
*   Hier können eigene Sounds eingebunden werden.
*   Ist in CfgSounds included.
*/

sounds[] = {};
class bug_growl_1
{
    // how the sound is referred to in the editor (e.g. trigger effects)
    name = "growl_1";

    // filename, volume, pitch, distance (optional)
    sound[] = { "USER\tunnel\growl_1.ogg", 1, 1, 500 };

    // subtitle delay in seconds, subtitle text
    titles[] = { 0, "" };
};

class bug_growl_2
{
    // how the sound is referred to in the editor (e.g. trigger effects)
    name = "growl_2";

    // filename, volume, pitch, distance (optional)
    sound[] = { "USER\tunnel\growl_2.ogg", 1, 1, 500 };

    // subtitle delay in seconds, subtitle text
    titles[] = { 0, "" };
};

class bug_growl_3
{
    // how the sound is referred to in the editor (e.g. trigger effects)
    name = "growl_3";

    // filename, volume, pitch, distance (optional)
    sound[] = { "USER\tunnel\growl_3.ogg", 1, 1, 500 };

    // subtitle delay in seconds, subtitle text
    titles[] = { 0, "" };
};